{
  "name": "obm",
  "private": true,
  "scripts": {
    "install:all": "nx run-many -t install",
    "build": "nx run-many -t build",
    "build:libs": "nx run-many -t build -p @obm/api-client @obm/domain @obm/ui-components"
  },
  "devDependencies": {
    "nx": "^21.6.5"
  }
}
